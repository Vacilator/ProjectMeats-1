# HTTP-only configuration for local development
# BACKEND_HOST will be substituted by entrypoint script

server {
    listen 80;
    server_name _;
    client_max_body_size 50M;

    root /usr/share/nginx/html;
    index index.html;

    # 1. Backend API Proxy
    location /api/ {
        proxy_pass http://${BACKEND_HOST}:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 2. Admin Panel Proxy
    location /admin/ {
        proxy_pass http://${BACKEND_HOST}:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 3. Static Files Proxy (Django Admin CSS)
    # Use ^~ priority modifier to prevent regex matches from overriding
    location ^~ /static/ {
        proxy_pass http://${BACKEND_HOST}:8000;
        proxy_set_header Host $host;
    }

    # 4. Media Files Proxy
    location /media/ {
        proxy_pass http://${BACKEND_HOST}:8000;
        proxy_set_header Host $host;
    }

    # 5. Cache Control for Frontend Assets (Vite content-hashed filenames)
    # Hashed assets are immutable — safe to cache long-term
    location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, no-transform";
    }

    # 6. Frontend SPA Routing (Fallback) - Must be last
    # index.html MUST NOT be cached — it contains references to current chunk hashes.
    # Stale index.html + new deployment = 404 on lazy-loaded chunks.
    location / {
        try_files $uri /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
    }
}
