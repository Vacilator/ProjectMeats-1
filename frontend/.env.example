# ⚠️ DEPRECATED - DO NOT USE

This file is obsolete. ProjectMeats now uses a manifest-based configuration system
with runtime configuration and domain-based auto-detection.

## 📖 READ THIS INSTEAD

**Primary Documentation:** [`docs/FRONTEND_ENVIRONMENT_VARIABLES.md`](../docs/FRONTEND_ENVIRONMENT_VARIABLES.md)

This comprehensive guide covers:
- ✅ Local development setup (`.env.local`)
- ✅ Environment-specific configuration (dev/uat/prod)
- ✅ All available variables and their defaults
- ✅ Runtime configuration (`env-config.js`)
- ✅ Domain-based auto-detection
- ✅ Best practices and troubleshooting

## Quick Start: Local Development

Create `frontend/.env.local` (not committed):

```bash
# API Configuration
REACT_APP_API_BASE_URL=http://localhost:8000/api/v1
REACT_APP_ENVIRONMENT=development

# Feature Flags
REACT_APP_AI_ASSISTANT_ENABLED=true
```

Then: `npm start`

## Environment-Specific API URLs

The app **auto-detects** from domain, but you can override:

- **Dev:** `https://dev-backend.meatscentral.com/api/v1`
- **UAT:** `https://uat.meatscentral.com/api/v1`
- **Prod:** `https://meatscentral.com/api/v1`
- **Local:** `http://localhost:8000/api/v1`

## Single Source of Truth

- **Manifest:** `config/env.manifest.json` (v3.3)
- **Frontend Docs:** `docs/FRONTEND_ENVIRONMENT_VARIABLES.md`
- **Secrets Guide:** `docs/CONFIGURATION_AND_SECRETS.md`
- **Audit Tool:** `python config/manage_env.py audit`

## Why This Changed

**Before:** Manual .env.example files → Secret drift, inconsistencies
**After:** Manifest + Runtime config + Auto-detection → Consistency & flexibility

## DO NOT

- ❌ Copy values from this file (it's deprecated)
- ❌ Commit secrets to `.env` files
- ❌ Hardcode API URLs in code
- ❌ Create new `.env.example` files

## DO

- ✅ Read `docs/FRONTEND_ENVIRONMENT_VARIABLES.md`
- ✅ Use `.env.local` for local dev (git-ignored)
- ✅ Use runtime config (`env-config.js`) for deployments
- ✅ Leverage domain-based auto-detection

---

**Last Updated:** December 10, 2025
**Manifest Version:** 3.3
**Full Documentation:** [`docs/FRONTEND_ENVIRONMENT_VARIABLES.md`](../docs/FRONTEND_ENVIRONMENT_VARIABLES.md)
