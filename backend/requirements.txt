# ProjectMeats Backend Requirements
# Django REST Framework backend for PowerApps migration
#
# For planned version upgrades, see docs/ROADMAP.md

# Core Django and DRF
Django>=5.0
djangorestframework>=3.15.0  # 3.15.0+ required for Django 6.0 compatibility
djangorestframework-simplejwt>=5.3.0  # JWT authentication (Wave S1)
django-cors-headers==4.9.0
django-environ==0.13.0
django-filter>=23.3  # Ensure latest compatible with Django 5.x

# Multi-tenancy: Uses shared-schema approach (NO django-tenants)
# All tenant isolation is via tenant_id foreign keys on business models

# Admin Enhancements (Bundle One: Custom System Data)
django-modal-actions==0.1.6  # Modal dialogs for Django Admin actions

# Database
psycopg[binary]>=3.2.0  # Modern PostgreSQL adapter (psycopg3)
dj-database-url==3.1.2

# API Documentation
drf-spectacular==0.29.0

# Development and Testing
django-extensions==4.1
pytest-django==4.12.0
pytest-cov==7.0.0
factory-boy==3.3.3

# Code Quality
black==26.1.0
flake8==7.3.0
isort==8.0.0
pre-commit==4.5.1

# Feature Flags (Wave 0)
django-flags==5.2.0

# Production (optional for development)
gunicorn==25.1.0
whitenoise==6.11.0

# Utilities
python-decouple==3.8
Pillow>=10.2.0  # For ImageField support if needed - 10.2.0+ supports Python 3.13
psutil>=5.9.0  # For system health monitoring

# HTTP requests
requests>=2.31.0  # For GitHub API integration in bug reports

# Email (SendGrid Web API)
sendgrid>=6.11.0
django-sendgrid-v5>=1.2.3

# OAuth and Social Auth (Email Integrations)
django-allauth>=0.61.0  # OAuth for Outlook/Microsoft and Gmail/Google
msal>=1.28.0  # Microsoft Authentication Library for advanced Outlook features
google-auth>=2.28.0  # Google auth for Gmail API
google-auth-oauthlib>=1.2.0  # OAuth helpers for Gmail
google-api-python-client>=2.120.0  # Gmail API client

# PDF Generation
reportlab>=4.0.0  # For generating PDF quotes and invoices

# Asynchronous Task Queue (Phase 5 Part 2)
celery[redis]>=5.4.0  # Task queue for scheduled workflows and background jobs
redis>=5.0.0  # Redis as Celery broker and result backend
django-celery-beat>=2.5.0  # Database-backed periodic task scheduler
bleach==6.1.0
sentry-sdk[django]>=2.0.0  # Error tracking and performance monitoring (Phase 6.4)
