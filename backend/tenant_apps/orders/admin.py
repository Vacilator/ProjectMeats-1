"""
Admin configuration for the orders app.

Currently empty as this app only provides abstract base classes.
Admin registration will be added when models are consolidated.
"""
from django.contrib import admin

# Admin classes will be added when order models are fully consolidated
